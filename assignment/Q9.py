"""Determine an expression for a sinusoid that oscillates 15 times per second, that has a value
of -1 at t = 0, and whose peak amplitude is 3.Use PYTHON to plot the signal over 0<=t<=1.
x(t) = 3sin(2π30t)
"""

import numpy as np
from matplotlib import pyplot as plt

